// Разработайте функцию findLargest, которая принимает три числа и возвращает наибольшее из них.

findLargest(3,4,5)

function findLargest(number, numberOne, numberTwo) {
    return Math.max(number, numberOne, numberTwo);
}